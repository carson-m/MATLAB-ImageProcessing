function Image = JpegDecoder(DCstream,ACstream,img_h,img_w)
    blockamount = ceil(img_h/8)*ceil(img_w/8);
    DCarray = DC
    ACarray = zeros(63,blockamount); % init ACsrray for i_zigzag
    % lenDCstream = length(DCstream);
    % lenACstream = length(ACstream);
    
    
end

